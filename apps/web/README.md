# Web (GitHub Pages)

## Rodar local

```bash
npm run dev --workspace apps/web
```

## Build

```bash
npm run build --workspace apps/web
```

Use `VITE_WAITLIST_ENDPOINT` para apontar para a Cloud Function `submitWaitlist`.
